# Antigravity Instructions - Haerriz_GoogleShoppingFeed

## Primary Rule
Read `ANTIGRAVITY_COMPLETE_FILE_STRUCTURE.md` before executing any phase tasks.
Follow the exact required phase order (P0 -> P1 -> P2 -> P3 -> P4 -> P5).

## Creation Rules
1. Never create empty placeholder files.
2. Never create unused service classes.
3. Complete features end-to-end (Admin/CLI -> Config -> Runtime Service -> Result -> Logging -> Verification).
4. Update `docs/IMPLEMENTATION_STATUS.md` after completing each phase.
5. Preserve existing functional code and avoid duplicating classes with names like `New*` or `Fixed*`.
